# Copy to .env.development and fill in real values.
# .env.development is gitignored – do NOT commit real credentials.

FFTT_APP_ID=
FFTT_PASSWORD=
FFTT_SERIE=
CLUB_NUMERO=08940073
CLUB_NOM=FONTENAYSIENNE
DATABASE_URL=postgresql://usftt:usftt@localhost:5433/usftt
PORT=3010

# Required in production; leave blank locally if you don't need CORS allowlisting.
ALLOWED_ORIGINS=

# Required to protect POST /api/sync/trigger/:module and GET /api/sync/logs/:jobName.
SYNC_TRIGGER_TOKEN=
