FFTT_APP_ID=
FFTT_PASSWORD=
FFTT_SERIE=
CLUB_NUMERO=08940073
CLUB_NOM=FONTENAYSIENNE
DATABASE_URL=postgresql://usftt:usftt@localhost:5432/usftt
PORT=3010
# Comma-separated list of allowed origins (e.g. https://usftt-results.example.com).
# Required in production; if unset the API only allows http://localhost:5180.
ALLOWED_ORIGINS=
# Bearer token required to call POST /api/sync/trigger/:module. If unset the
# endpoint is unauthenticated, which is only suitable for local development.
SYNC_TRIGGER_TOKEN=
ACME_EMAIL=
