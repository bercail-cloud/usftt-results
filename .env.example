FFTT_APP_ID=
FFTT_PASSWORD=
FFTT_SERIE=
CLUB_NUMERO=08940073
CLUB_NOM=FONTENAYSIENNE
DATABASE_URL=postgresql://usftt:usftt@localhost:5433/usftt
PORT=3010

# Optional, but required in production:
# Comma-separated list of allowed origins (CORS). When unset in production the
# server refuses to start to prevent the default of "*".
ALLOWED_ORIGINS=

# Optional, but required in production:
# Bearer token expected on POST /api/sync/trigger/:module. When unset in
# production the trigger endpoint is disabled (returns 503).
SYNC_TRIGGER_TOKEN=

# Set to "production" on prod deployments to enable strict env validation.
NODE_ENV=development

ACME_EMAIL=
